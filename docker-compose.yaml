networks:
  dev:

services:
  redis:
    image: redis:alpine
    env_file:
      - ./.env
    restart: on-failure
    ports:
      - "6379:6379"
    networks:
      - dev
  bot:
    build:
      dockerfile: Dockerfile
    env_file:
      - ./.env
    restart: always
    depends_on:
      - redis
    networks:
      - dev
